{
  "name": "cadastro-usuarios",
  "author": "Daniel Soares <daniel.soares@me.com>",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "build:back": "lerna run build --scope backend",
    "build:front": "lerna run build --scope frontend",
    "start": "lerna run start",
    "start:back": "lerna run start --scope backend",
    "start:front": "lerna run start --scope frontend",
    "prisma:push": "lerna run prisma:push --scope backend",
    "prisma:seed": "lerna run prisma:seed --scope backend",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
